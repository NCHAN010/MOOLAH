import { Time } from '@angular/common';
//ng import {NgbCalendar, NgbDate} from "@ng-bootstrap/ng-bootstrap";
//import * as moment from 'moment';
//import 'moment-duration-format';
export class primaryCom {

	orgId:string;

     userId:string;
	status:string;
	moolahTitle:string;
	limit:number;
 	currAmt:number;
	no_OfSecCom:number;
	date_created:Date;
	 endgoal_date:Date;
	// duration :moment.duration(decimalHours, 'hours') ; 

}